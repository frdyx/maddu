// `maddu upgrade` — pull newer framework files in place; never touch project state.
// Slice 1 stub. Real implementation lands in Slice 4.

export default async function upgrade(_args) {
  console.log('maddu upgrade: not yet implemented (scaffold-only stub, Slice 1).');
  console.log('Planned for Slice 4. See docs/upgrade-policy.md.');
  process.exit(0);
}
