// `maddu start` — boot the bridge server on 127.0.0.1:4177.
// Slice 1 stub. Real implementation lands in Slice 2.

export default async function start(_args) {
  console.log('maddu start: not yet implemented (scaffold-only stub, Slice 1).');
  console.log('Planned for Slice 2. Bridge server will live at template/maddu/runtime/server.js.');
  process.exit(0);
}
