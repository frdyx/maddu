// `maddu init` — scaffold .maddu/ + the runtime into the target directory.
// Slice 1 stub. Real implementation lands in Slice 4.

export default async function init(_args) {
  console.log('maddu init: not yet implemented (scaffold-only stub, Slice 1).');
  console.log('Planned for Slice 4. See docs/maddu-v0.3-roadmap.md.');
  process.exit(0);
}
